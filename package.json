{
  "name": "@f/parse-color",
  "description": "Parse a valid CSS color string into an array of [red, green, blue, alpha]",
  "repository": "git://github.com/micro-js/parse-color.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/hex-to-rgba": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
